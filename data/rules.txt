Проектная документация
Выполнил: Федоренко Евгений Игоревич, группа ПА-01

Правила преобразования данных (Preprocessing Rules):

1. Текстовые признаки (Категории):
   - Столбцы 'Type' и 'Socket' преобразуются в числа через Label Encoding.
   - Столбец 'Name' (Название) удаляется.

2. Числовые признаки:
   - Столбцы 'TDP' и 'Price' нормализуются через MinMax Scaler (в диапазон [0, 1]).

3. Целевая метка (Target):
   - Столбец 'Compatible' остается бинарным (1 или 0).
